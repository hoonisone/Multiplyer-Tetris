#pragma once
#include <stdio.h>

char* newChar(char value);
int* newInt(int value);
float* newFloat(float value);
double* newDouble(double value);
char* newString(char* value);
int isInRange(int X, int Y, int W, int H, int x, int y);